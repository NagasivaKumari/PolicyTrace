/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'
import { AppFactory as _AppFactory, AppFactoryAppClientParams, AppFactoryResolveAppClientByCreatorAndNameParams, AppFactoryDeployParams, AppFactoryParams, CreateSchema } from '@algorandfoundation/algokit-utils/types/app-factory'
import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'
import SimulateResponse = modelsv2.SimulateResponse

export const APP_SPEC: Arc56Contract = {"name":"BlockdAudit","structs":{},"methods":[{"name":"create","args":[],"returns":{"type":"void"},"actions":{"create":["NoOp"],"call":[]},"readonly":false,"events":[],"recommendations":{}},{"name":"anchor_audit","args":[{"type":"string","name":"url_hash"},{"type":"uint64","name":"risk_score"},{"type":"string","name":"ipfs_cid"},{"type":"string","name":"report_hash"}],"returns":{"type":"string"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Anchor an audit record on-chain.","events":[],"recommendations":{}},{"name":"get_total_audits","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Read total number of audits anchored.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"total_audits":{"keyType":"AVMString","valueType":"AVMUint64","key":"dG90YWxfYXVkaXRz"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":[],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[136,183],"errorMessage":"check self.total_audits exists"},{"pc":[86,110,126],"errorMessage":"invalid array length header"},{"pc":[93,117,132],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[104],"errorMessage":"invalid number of bytes for arc4.uint64"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"source":{"approval":"","clear":"I3ByYWdtYSB2ZXJzaW9uIDExCiNwcmFnbWEgdHlwZXRyYWNrIGZhbHNlCgovLyBhbGdvcHkuYXJjNC5BUkM0Q29udHJhY3QuY2xlYXJfc3RhdGVfcHJvZ3JhbSgpIC0+IHVpbnQ2NDoKbWFpbjoKICAgIHB1c2hpbnQgMQogICAgcmV0dXJuCg=="},"byteCode":{"approval":"CyADAAECJgIMdG90YWxfYXVkaXRzBBUffHUxGEAAAygiZzEZFEQxGEEAFoICBHswyrEE4dblazYaAI4CABEAdACABExcYbo2GgCOAQABACNDNhoBSSJZJAhLARUSRFcCADYaAhWBCBJENhoDSSJZJAhLARUSRFcCADYaBEkiWSQITBUSRCIoZUQjCChMZ4ANQkxPQ0tEX0FVRElUOk8CUIABOlBMUEkVFlcGAkxQKUxQsCNDIihlRBYpTFCwI0M=","clear":"C4EBQw=="},"compilerInfo":{"compiler":"puya","compilerVersion":{"major":5,"minor":7,"patch":1}},"events":[],"templateVariables":{}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the BlockdAudit contract
 */
export type BlockdAuditArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'create()void': Record<string, never>
    'anchor_audit(string,uint64,string,string)string': {
      urlHash: string
      riskScore: bigint | number
      ipfsCid: string
      reportHash: string
    }
    'get_total_audits()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'create()void': []
    'anchor_audit(string,uint64,string,string)string': [urlHash: string, riskScore: bigint | number, ipfsCid: string, reportHash: string]
    'get_total_audits()uint64': []
  }
}

/**
 * The return type for each method
 */
export type BlockdAuditReturns = {
  'create()void': void
  'anchor_audit(string,uint64,string,string)string': string
  'get_total_audits()uint64': bigint
}

/**
 * Defines the types of available calls and state of the BlockdAudit smart contract.
 */
export type BlockdAuditTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'create()void' | 'create', {
      argsObj: BlockdAuditArgs['obj']['create()void']
      argsTuple: BlockdAuditArgs['tuple']['create()void']
      returns: BlockdAuditReturns['create()void']
    }>
    & Record<'anchor_audit(string,uint64,string,string)string' | 'anchor_audit', {
      argsObj: BlockdAuditArgs['obj']['anchor_audit(string,uint64,string,string)string']
      argsTuple: BlockdAuditArgs['tuple']['anchor_audit(string,uint64,string,string)string']
      returns: BlockdAuditReturns['anchor_audit(string,uint64,string,string)string']
    }>
    & Record<'get_total_audits()uint64' | 'get_total_audits', {
      argsObj: BlockdAuditArgs['obj']['get_total_audits()uint64']
      argsTuple: BlockdAuditArgs['tuple']['get_total_audits()uint64']
      returns: BlockdAuditReturns['get_total_audits()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        totalAudits: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type BlockdAuditSignatures = keyof BlockdAuditTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type BlockdAuditNonVoidMethodSignatures = keyof BlockdAuditTypes['methods'] extends infer T ? T extends keyof BlockdAuditTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the BlockdAudit smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends BlockdAuditSignatures> = BlockdAuditTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the BlockdAudit smart contract to the method's return type
 */
export type MethodReturn<TSignature extends BlockdAuditSignatures> = BlockdAuditTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = BlockdAuditTypes['state']['global']['keys']


/**
 * Defines supported create method params for this smart contract
 */
export type BlockdAuditCreateCallParams =
  | Expand<CallParams<BlockdAuditArgs['obj']['create()void'] | BlockdAuditArgs['tuple']['create()void']> & {method: 'create'} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
  | Expand<CallParams<BlockdAuditArgs['obj']['create()void'] | BlockdAuditArgs['tuple']['create()void']> & {method: 'create()void'} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
/**
 * Defines arguments required for the deploy method.
 */
export type BlockdAuditDeployParams = Expand<Omit<AppFactoryDeployParams, 'createParams' | 'updateParams' | 'deleteParams'> & {
  /**
   * Create transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  createParams?: BlockdAuditCreateCallParams
}>


/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the BlockdAudit smart contract
 */
export abstract class BlockdAuditParamsFactory {
  /**
   * Gets available create ABI call param factories
   */
  static get create() {
    return {
      _resolveByMethod<TParams extends BlockdAuditCreateCallParams & {method: string}>(params: TParams) {
        switch(params.method) {
          case 'create':
          case 'create()void':
            return BlockdAuditParamsFactory.create.create(params)
        }
        throw new Error(`Unknown ' + verb + ' method`)
      },

      /**
       * Constructs create ABI call params for the BlockdAudit smart contract using the create()void ABI method
       *
       * @param params Parameters for the call
       * @returns An `AppClientMethodCallParams` object for the call
       */
      create(params: CallParams<BlockdAuditArgs['obj']['create()void'] | BlockdAuditArgs['tuple']['create()void']> & AppClientCompilationParams & {onComplete?: OnApplicationComplete.NoOpOC}): AppClientMethodCallParams & AppClientCompilationParams & {onComplete?: OnApplicationComplete.NoOpOC} {
        return {
          ...params,
          method: 'create()void' as const,
          args: Array.isArray(params.args) ? params.args : [],
        }
      },
    }
  }

  /**
   * Constructs a no op call for the anchor_audit(string,uint64,string,string)string ABI method
   *
   * Anchor an audit record on-chain.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static anchorAudit(params: CallParams<BlockdAuditArgs['obj']['anchor_audit(string,uint64,string,string)string'] | BlockdAuditArgs['tuple']['anchor_audit(string,uint64,string,string)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'anchor_audit(string,uint64,string,string)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.urlHash, params.args.riskScore, params.args.ipfsCid, params.args.reportHash],
    }
  }
  /**
   * Constructs a no op call for the get_total_audits()uint64 ABI method
   *
   * Read total number of audits anchored.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalAudits(params: CallParams<BlockdAuditArgs['obj']['get_total_audits()uint64'] | BlockdAuditArgs['tuple']['get_total_audits()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_total_audits()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A factory to create and deploy one or more instance of the BlockdAudit smart contract and to create one or more app clients to interact with those (or other) app instances
 */
export class BlockdAuditFactory {
  /**
   * The underlying `AppFactory` for when you want to have more flexibility
   */
  public readonly appFactory: _AppFactory

  /**
   * Creates a new instance of `BlockdAuditFactory`
   *
   * @param params The parameters to initialise the app factory with
   */
  constructor(params: Omit<AppFactoryParams, 'appSpec'>) {
    this.appFactory = new _AppFactory({
      ...params,
      appSpec: APP_SPEC,
    })
  }
  
  /** The name of the app (from the ARC-32 / ARC-56 app spec or override). */
  public get appName() {
    return this.appFactory.appName
  }
  
  /** The ARC-56 app spec being used */
  get appSpec() {
    return APP_SPEC
  }
  
  /** A reference to the underlying `AlgorandClient` this app factory is using. */
  public get algorand(): AlgorandClient {
    return this.appFactory.algorand
  }
  
  /**
   * Returns a new `AppClient` client for an app instance of the given ID.
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public getAppClientById(params: AppFactoryAppClientParams) {
    return new BlockdAuditClient(this.appFactory.getAppClientById(params))
  }
  
  /**
   * Returns a new `AppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public async getAppClientByCreatorAndName(
    params: AppFactoryResolveAppClientByCreatorAndNameParams,
  ) {
    return new BlockdAuditClient(await this.appFactory.getAppClientByCreatorAndName(params))
  }

  /**
   * Idempotently deploys the BlockdAudit smart contract.
   *
   * @param params The arguments for the contract calls and any additional parameters for the call
   * @returns The deployment result
   */
  public async deploy(params: BlockdAuditDeployParams = {}) {
    const result = await this.appFactory.deploy({
      ...params,
      createParams: params.createParams?.method ? BlockdAuditParamsFactory.create._resolveByMethod(params.createParams) : params.createParams ? params.createParams as (BlockdAuditCreateCallParams & { args: Uint8Array[] }) : undefined,
    })
    return { result: result.result, appClient: new BlockdAuditClient(result.appClient) }
  }

  /**
   * Get parameters to create transactions (create and deploy related calls) for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the BlockdAudit smart contract using the create()void ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The create params
       */
      create: (params: CallParams<BlockdAuditArgs['obj']['create()void'] | BlockdAuditArgs['tuple']['create()void']> & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
        return this.appFactory.params.create(BlockdAuditParamsFactory.create.create(params))
      },
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the BlockdAudit smart contract using the create()void ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The create transaction
       */
      create: (params: CallParams<BlockdAuditArgs['obj']['create()void'] | BlockdAuditArgs['tuple']['create()void']> & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
        return this.appFactory.createTransaction.create(BlockdAuditParamsFactory.create.create(params))
      },
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the BlockdAudit smart contract using an ABI method call using the create()void ABI method.
       *
       * @param params The params for the smart contract call
       * @returns The create result
       */
      create: async (params: CallParams<BlockdAuditArgs['obj']['create()void'] | BlockdAuditArgs['tuple']['create()void']> & AppClientCompilationParams & CreateSchema & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
        const result = await this.appFactory.send.create(BlockdAuditParamsFactory.create.create(params))
        return { result: { ...result.result, return: result.result.return as unknown as (undefined | BlockdAuditReturns['create()void']) }, appClient: new BlockdAuditClient(result.appClient) }
      },
    },

  }

}
/**
 * A client to make calls to the BlockdAudit smart contract
 */
export class BlockdAuditClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `BlockdAuditClient`
   *
   * @param appClient An `AppClient` instance which has been created with the BlockdAudit app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `BlockdAuditClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }
  
  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends BlockdAuditNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }
  
  /**
   * Returns a new `BlockdAuditClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<BlockdAuditClient> {
    return new BlockdAuditClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `BlockdAuditClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<BlockdAuditClient> {
    return new BlockdAuditClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the BlockdAudit smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the BlockdAudit smart contract using the `anchor_audit(string,uint64,string,string)string` ABI method.
     *
     * Anchor an audit record on-chain.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    anchorAudit: (params: CallParams<BlockdAuditArgs['obj']['anchor_audit(string,uint64,string,string)string'] | BlockdAuditArgs['tuple']['anchor_audit(string,uint64,string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(BlockdAuditParamsFactory.anchorAudit(params))
    },

    /**
     * Makes a call to the BlockdAudit smart contract using the `get_total_audits()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read total number of audits anchored.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalAudits: (params: CallParams<BlockdAuditArgs['obj']['get_total_audits()uint64'] | BlockdAuditArgs['tuple']['get_total_audits()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(BlockdAuditParamsFactory.getTotalAudits(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the BlockdAudit smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the BlockdAudit smart contract using the `anchor_audit(string,uint64,string,string)string` ABI method.
     *
     * Anchor an audit record on-chain.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    anchorAudit: (params: CallParams<BlockdAuditArgs['obj']['anchor_audit(string,uint64,string,string)string'] | BlockdAuditArgs['tuple']['anchor_audit(string,uint64,string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(BlockdAuditParamsFactory.anchorAudit(params))
    },

    /**
     * Makes a call to the BlockdAudit smart contract using the `get_total_audits()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read total number of audits anchored.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalAudits: (params: CallParams<BlockdAuditArgs['obj']['get_total_audits()uint64'] | BlockdAuditArgs['tuple']['get_total_audits()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(BlockdAuditParamsFactory.getTotalAudits(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the BlockdAudit smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the BlockdAudit smart contract using the `anchor_audit(string,uint64,string,string)string` ABI method.
     *
     * Anchor an audit record on-chain.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    anchorAudit: async (params: CallParams<BlockdAuditArgs['obj']['anchor_audit(string,uint64,string,string)string'] | BlockdAuditArgs['tuple']['anchor_audit(string,uint64,string,string)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(BlockdAuditParamsFactory.anchorAudit(params))
      return {...result, return: result.return as unknown as (undefined | BlockdAuditReturns['anchor_audit(string,uint64,string,string)string'])}
    },

    /**
     * Makes a call to the BlockdAudit smart contract using the `get_total_audits()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Read total number of audits anchored.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalAudits: async (params: CallParams<BlockdAuditArgs['obj']['get_total_audits()uint64'] | BlockdAuditArgs['tuple']['get_total_audits()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(BlockdAuditParamsFactory.getTotalAudits(params))
      return {...result, return: result.return as unknown as (undefined | BlockdAuditReturns['get_total_audits()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new BlockdAuditClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the BlockdAudit smart contract using the `get_total_audits()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Read total number of audits anchored.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getTotalAudits(params: CallParams<BlockdAuditArgs['obj']['get_total_audits()uint64'] | BlockdAuditArgs['tuple']['get_total_audits()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(BlockdAuditParamsFactory.getTotalAudits(params))
    return result.return as unknown as BlockdAuditReturns['get_total_audits()uint64']
  }

  /**
   * Methods to access state for the current BlockdAudit app
   */
  state = {
    /**
     * Methods to access global state for the current BlockdAudit app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalAudits: result.total_audits,
        }
      },
      /**
       * Get the current value of the total_audits key in global state
       */
      totalAudits: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_audits")) as bigint | undefined },
    },
  }

  public newGroup(): BlockdAuditComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a anchor_audit(string,uint64,string,string)string method call against the BlockdAudit contract
       */
      anchorAudit(params: CallParams<BlockdAuditArgs['obj']['anchor_audit(string,uint64,string,string)string'] | BlockdAuditArgs['tuple']['anchor_audit(string,uint64,string,string)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.anchorAudit(params)))
        resultMappers.push((v) => client.decodeReturnValue('anchor_audit(string,uint64,string,string)string', v))
        return this
      },
      /**
       * Add a get_total_audits()uint64 method call against the BlockdAudit contract
       */
      getTotalAudits(params: CallParams<BlockdAuditArgs['obj']['get_total_audits()uint64'] | BlockdAuditArgs['tuple']['get_total_audits()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalAudits(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_total_audits()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the BlockdAudit contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as BlockdAuditComposer
  }
}
export type BlockdAuditComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the anchor_audit(string,uint64,string,string)string ABI method.
   *
   * Anchor an audit record on-chain.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  anchorAudit(params?: CallParams<BlockdAuditArgs['obj']['anchor_audit(string,uint64,string,string)string'] | BlockdAuditArgs['tuple']['anchor_audit(string,uint64,string,string)string']>): BlockdAuditComposer<[...TReturns, BlockdAuditReturns['anchor_audit(string,uint64,string,string)string'] | undefined]>

  /**
   * Calls the get_total_audits()uint64 ABI method.
   *
   * Read total number of audits anchored.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalAudits(params?: CallParams<BlockdAuditArgs['obj']['get_total_audits()uint64'] | BlockdAuditArgs['tuple']['get_total_audits()uint64']>): BlockdAuditComposer<[...TReturns, BlockdAuditReturns['get_total_audits()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the BlockdAudit smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): BlockdAuditComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): BlockdAuditComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<BlockdAuditComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<BlockdAuditComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<BlockdAuditComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<BlockdAuditComposerResults<TReturns>>
}
export type BlockdAuditComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

